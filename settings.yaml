system:
#[GIN-debug] [WARNING] Running in "debug" mode. Switch to "release" mode in production.
# - using env:   export GIN_MODE=release
# - using code:  gin.SetMode(gin.ReleaseMode)


    #mode: release   # mode 意思是显示不显示debug
    ip: 127.0.0.1
    port: 8090
db:
    mode: mysql
    host: 192.168.10.199
    port: 3306
    user: root
    password: Zdj_7819!
    db_name: rbacAdmin

redis:
    addr: 192.168.10.199:6379
    password: 
    db: 3

jwt:
    secret: "aB3kL9mN7xY2qR8sT1uV4wE6zC0pF5gH"
    expire: 72  # 延长到72小时（3天）
    issuer: "rbacAdmin"
   
captcha:
    enable: false

email:
    user: 315420498@qq.com  # 发送者邮箱地址
    password: zmkbniplmiqmbjag  # 授权密码
    Host: smtp.qq.com  # 邮箱服务器
    Port: 465  # 端口

upload:
    size: 10  # 10MB
    ext: ".jpg,.jpeg,.png,.gif,.bmp"  # 允许的文件扩展名
    dir: file  # 上传目录